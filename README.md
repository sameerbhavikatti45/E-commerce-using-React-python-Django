# E-commerce-using-React-python-Django
### Important Note First install node_modules folder To run this code first run the server

To run this code first run the server 

1. check your ternimal/cmd is in right root directory
2. after confimation you can run run server by this command below this 
### `npm start`

The page will reload when you make changes.\
You may also see any lint errors in the console.

# To run the frontend 
  Go to the root folder (frontend folder) and start the localhost server 
  # npm start 
#  To run the backend 
  Go to the root folder (backend folder) and start the server 
  # python manage.py runserver

### To access you admin panel
 Create your own admin credientials
   # python manage.py createsuperuser
